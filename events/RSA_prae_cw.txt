add_namespace = rusrev



country_event = {

id = rusrev.1
title = rusrev.1.t
desc = rusrev.1.d
hidden = no
picture = GFX_report_event_generic_battle
fire_only_once = yes
is_triggered_only = yes
option = {
        create_faction = "Eastern Entente"
        set_politics = {

            ruling_party = democratic
            elections_allowed = yes
            last_election = 1905.01.01
            election_frequency = 48


        }


add_ideas = February_Revolution


        }
}


add_namespace = RCO

country_event = {

id = RCO.001
title = RCO.001.t
desc = RCO.001.d
hidden = no
picture = GFX_report_event_generic_battle
fire_only_once = yes
is_triggered_only = yes

option = {

  
GER = {every_state = { limit = { is_controlled_by = GER
                        is_owned_by = RSA

 }
  transfer_state_to = GER } 
}

AUS = {every_state = { limit = { is_controlled_by = AUS
                        is_owned_by = RSA

 }
  transfer_state_to = GER } 
}

BUL = {every_state = { limit = { is_controlled_by = BUL
                        is_owned_by = RSA

 }
  transfer_state_to = GER } 
}

TUR = {every_state = { limit = { is_controlled_by = TUR
                        is_owned_by = RSA

 }
  transfer_state_to = GER } 
}

every_state = { limit = { 
                        is_owned_by = RSA

 }
  transfer_state_to = SOV }   

SOV = {
  
declare_war_on = {
                target = GER
                type = annex_everything
            }
declare_war_on = {
                target = BUL
                type = annex_everything
            }
declare_war_on = {
                target = TUR
                type = annex_everything
            }
declare_war_on = {
                target = AUS
                type = annex_everything
      }
    }
  }
}

add_namespace = NFT

country_event = {
id = NFT.1
title = NFT.1.t
desc = NFT.1.d

is_triggered_only = yes 

option = {

name = "We will lead the people to freedom"

load_focus_tree = {

tree = RSADEM_focus

}

  } 
}


GER_Great_War = {
	GER_capture_cities = {

		priority = 10

		icon = generic_ignite_civil_war
		fire_only_once = yes

		visible = {

		}
	
		available = {
			has_war = yes
			AND = {
				16 = { is_fully_controlled_by = GER }
				
			}
		}
		cancel_trigger = {
			has_war = no
		}
		icon = mission_icon     # For GFX_decision_mission_icon
		is_good = yes
		days_mission_timeout = 680

		selectable_mission = yes
		complete_effect = {
			add_stability = 0.10
		}
		timeout_effect = {
			country_event = TOV.001
		}
	}
}